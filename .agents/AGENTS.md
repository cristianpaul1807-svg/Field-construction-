# Project Rules for Field Construction

1. **Local Development Only**: Never run `git push`, deploy code, or modify remote production environments without explicit permission from the user.
2. **Secrets Protection**: Never commit or track `.env`, API keys, or credentials in git.
